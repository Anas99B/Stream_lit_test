version: v1

components:
  - name: prompt-builder
    docker:
      file: Dockerfile

    resources:
      cpu: 500m
      memory: 1Gi

    ingresses:
      - host: prompt-builder

    ports:
      - 8080
